services:
  mongodb:
    image: mongo:8
    volumes:
      - mongo_data:/data/db

  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      MONGODB_URI: mongodb://mongodb:27017/ticketsdb
    depends_on:
      - mongodb

volumes:
  mongo_data: